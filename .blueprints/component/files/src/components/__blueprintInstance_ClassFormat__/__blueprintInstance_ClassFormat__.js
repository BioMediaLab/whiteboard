import React from 'react'
import './{{blueprintInstance_ClassFormat}}.css'

export default function {{blueprintInstance_ClassFormat}}({ children }) {
  return (
    <div className="{{blueprintInstance_ClassFormat}}">
      {children}
    </div>
  )
}
